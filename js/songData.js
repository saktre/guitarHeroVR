
// Codes to load the song arrays for comparison

// Run in console to take the values
// from training and save to JSON
// saveJSON(songData) // then write to a file

/*
var songData = {
  green:greenPts,
  red:redPts,
  yellow:yellowPts,
  blue:bluePts
}
*/

// var songData = loadJSON("static/slowDownData.json")
